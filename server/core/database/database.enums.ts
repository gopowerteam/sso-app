export enum PaginatorMode {
  Cursor = 'CURSOR',
  Index = 'INDEX',
}

export enum OrderMode {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum WhereOperator {
  In = 'IN',
  Like = 'LIKE',
  Equal = 'EQUAL',
  Between = 'BETWEEN',
}
